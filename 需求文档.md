# lulu-bootstrap — 全局需求文档（Requirements）

> 版本：v2（精简版）
> 日期：2026-08-12
> 范围：整个 `d:/code/lulu-bootstrap` 仓库（声明式应用启动器）

---

## 1. 背景与目标

`lulu-bootstrap` 是一个**声明式应用启动器（declarative bootstrapper）**。它读取一份统一的配置（YAML/JSON/二进制），将 Go 应用所需的各类子系统（配置源、传输层、日志、追踪、指标、对象存储、缓存、数据库）组装成一个可运行的 `lulu.App`，并提供优雅停机与资源清理。

**核心目标**：
- 用一份强类型 Schema（`conf` proto）替代散落各处的硬编码配置结构。
- 通过 **builder 注册表 + 子包自注册** 模式，让每个子系统可插拔、零侵入启用。
- 应用开发者只需写一份配置 + 匿名导入所需子系统包，即可获得完整运行基线。

**非目标**：
- 不实现子系统底层能力（委托 `lulu` / `lulu-ext` / `go-crud` 插件）。
- 不在此阶段实现 AI / 消息代理 / 注册中心 / 脚本引擎 / 工作流（v2 已收敛，见 §3）。

---

## 2. 利益相关方

| 角色 | 关注点 |
|------|--------|
| 应用开发者 | 一份配置 + 匿名导入即可启动应用 |
| 框架维护者 | Schema 兼容演进、builder 可插拔 |
| 平台/运维 | 配置可被配置中心/K8s/CI 渲染下发 |
| 工具链 | proto 可被 buf / 文档 / 校验器消费 |

---

## 3. 功能需求总览

### FR-1 顶层 BootstrapConfig（9 子域）
顶层配置 `BootstrapConfig`（`conf`）包含且只包含 9 个子域，字段 1–9：
`app` / `server` / `config` / `logger` / `tracer` / `metrics` / `storage` / `cache` / `database`。

> 相较 v1 收敛：移除 registry / ai / broker / script / workflow 五大域。

### FR-2 应用元数据（app）
解析为 `lulu.Option`：`id` / `name` / `version`；`stop_timeout` 缺省在 `env=prd` 时为 30s。

### FR-3 配置源（config）— 2 种
- `file`：本地文件 + fsnotify 监听。
- `nacos`：Nacos 配置中心。
- 支持混合挂载（同一应用同时挂多个源）。

> 移除：etcd/consul/apollo/kubernetes/redis/zookeeper/vault/http/env/oss/polaris。

### FR-4 传输层（server）— HTTP
- `http`：addr（缺省 `:8080`）、内置 std Driver、声明式中间件（Recovery→CORS→Logging→RequestID→Tracing→RateLimit→Timeout）+ 命令式路由 Setup。
- 仅 HTTP；移除 gRPC/GraphQL/WebSocket/消息队列 Server 等。

### FR-5 日志（logger）— Slog
- `slog`：level / format / output_path（Go 标准库）。

### FR-6 追踪（tracer）— OTLP
- `otlp`：endpoint / insecure / headers / sampler / sample_ratio。

### FR-7 指标（metrics）— Prometheus / OTLP
- `prometheus`：addr / path；`otlp`：endpoint / insecure / headers / push_interval。

### FR-8 对象存储（storage）— MinIO / S3
- `minio` / `s3`（S3 兼容，含 region / force_path_style / bucket）。

### FR-9 缓存（cache）— Local / Redis
- `local`（FreeCache）/ `redis`。

### FR-10 数据库（database）— GORM / Elasticsearch
- `sql`（GORM/Ent 风格，含自动迁移）/ `elasticsearch`。

### FR-11 Builder 注册表与子包自注册
- 每个子系统域维护 `map[string]BuilderFunc`，key 为小写类型字符串（`types.go` 常量）。
- 子包在 `init()` 调 `bootstrap.MustRegisterXxxBuilder(typeKey, fn)` 完成注册，业务以 `import _` 启用。
- 未注册类型返回"未注册"错误。

### FR-12 配置加载与解码
- 支持 YAML（`.yaml`/`.yml` → `YAMLToJSON` → `protojson`）、JSON（`.json` → `protojson`）、二进制（`.bin`/`.pb` → `proto.Unmarshal`）。
- CLI 通过 `--conf/-c` 指定路径，缺省 `config.yaml`（cobra）。

### FR-13 资源清理
- 每个 builder 返回 `cleanup func()`；框架在 `Context.Cleanup()`（`sync.Once` 保证幂等）中按注册逆序回收，任一环节失败时回滚已注册 cleanup。

### FR-14 代码生成工具链
- 使用 buf v2，module 路径 `protos`，生成 Go 至 `conf/gen/go`（managed mode + `paths=source_relative`）。
- `Makefile` 提供 `make api`（`cd conf && buf generate`）。

### 待恢复域（v2 暂移除，conf 中曾定义）
`ai`（OpenAI/LangChainGo/Eino）、`broker`（Kafka 等 13 种）、`registry`（Consul 等 8 种）、`script`（9 种引擎）、`workflow`（4 种）。后续可随 proto 加回 + 补对应适配层。

---

## 4. 非功能需求

| 编号 | 需求 |
|------|------|
| NFR-1 | **声明式**：拓扑全部由配置描述。 |
| NFR-2 | **可插拔/零侵入**：`import _` 启用子系统，无注册样板。 |
| NFR-3 | **强类型契约**：基于 proto3 + `optional`，编译期校验。 |
| NFR-4 | **兼容演进**：新增域/源不改现有字段编号；保留编号不复用。 |
| NFR-5 | **优雅停机**：`stop_timeout` + cleanup 逆序回收。 |
| NFR-6 | **生成代码纯净**：`conf/gen` 不依赖业务/插件库。 |
| NFR-7 | **依赖隔离**：子模块（database/gorm、database/elasticsearch 等）独立 go.mod。 |

---

## 5. 仓库目录结构

```
lulu-bootstrap/
├── bootstrap.go          # 顶层 Bootstrap() + 配置加载/解码
├── builder.go            # 各域 builder 注册表 + 常量引用
├── types.go              # 类型 key 常量（ServerType*/ConfigType*/...）
├── context.go            # 运行期 Context 封装（cancel/cleanup/storages/caches/databases）
├── cli.go                # cobra 命令与 --conf 标志
├── *_builder.go          # 各域聚合解析（app/log/metrics/tracer/storage/cache/server）
├── conf/                 # 配置契约层（proto + buf + gen）—— 见 conf/需求文档.md
│   ├── protos/bootstrap/v1/   # 10 个 .proto（唯一事实源）
│   └── gen/go/bootstrap/v1/   # 10 个生成 .pb.go
├── config/               # 配置源适配层（file、nacos）—— 见 config/需求文档.md
├── database/             # 数据库适配层（gorm、elasticsearch）—— 见 database/需求文档.md
├── oss/                  # 对象存储适配层（minio、s3）—— 见 oss/需求文档.md
├── transport/            # 传输层适配层（http）—— 见 transport/需求文档.md
├── cache/                # 缓存适配层（local、redis）
├── Makefile              # make api → buf generate
└── go.mod                # 根模块（聚合依赖）
```

> 注：`config_builder.go`、`database_builder.go` 旧顶层聚合文件已删除；聚合逻辑现位于各 `*_builder.go` 与 `bootstrap.go`。

---

## 6. 验收标准

1. `make api` 成功生成 10 个 `.pb.go`，`buf lint`/`buf breaking` 通过。
2. 一份 YAML 声明全部 9 子域，经 `Bootstrap` 解析为运行中的 `lulu.App`。
3. 业务仅需 `import _ ".../config/nacos"`、`import _ ".../transport/http"` 等即可启用对应子系统，无额外注册代码。
4. 应用收到停机信号时，所有子系统按逆序优雅清理。
5. 新增任意配置源/引擎仅通过"新增 proto optional + 注册 builder"，不改已发布字段编号。

---

## 7. 风险与假设

- **假设**：`bootstrap.v1` 为首个稳定主版本；v2 收敛为最小核心。
- **风险**：移除 5 大域并重排顶层字段编号属破坏性变更，旧 YAML 需按新编号重写；建议以本轮提交为 `buf breaking` 新基线。
- **风险**：多个适配层 `cleanup` 为空函数（oss/minio、oss/s3、nacos、es），长连接回收依赖进程退出。
- **注意**：`transport/http` 的 conf `driver` 字段（chi/gin/fiber）当前未消费，始终用内置 std Driver。
