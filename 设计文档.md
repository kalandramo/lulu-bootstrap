# lulu-bootstrap — 全局设计文档（Design）

> 版本：v2（精简版）
> 日期：2026-08-12
> 范围：整个 `d:/code/lulu-bootstrap` 仓库（声明式应用启动器设计）

---

## 1. 架构总览

`lulu-bootstrap` 采用**契约 + 适配 + 注册表**三层架构：

```
┌────────────────────────────────────────────────────────────┐
│ 用户配置 (YAML / JSON / .bin)                                │
└───────────────┬────────────────────────────────────────────┘
                │ LoadConfigFromFile → protojson / proto.Unmarshal
                ▼
┌────────────────────────────────────────────────────────────┐
│ 契约层 conf/gen/go/bootstrap/v1  (proto 生成, 强类型)        │
└───────────────┬────────────────────────────────────────────┘
                │ Bootstrap(ctx, cfg) 按序 resolveXxx
                ▼
┌────────────────────────────────────────────────────────────┐
│ 聚合层 bootstrap 包 (*_builder.go)                           │
│  resolveApp→resolveLog→resolveServer→resolveConfig→         │
│  resolveTracer→resolveMetrics→resolveStorage→resolveCache→  │
│  resolveDatabase → lulu.New(opts...)                         │
└───────────────┬────────────────────────────────────────────┘
                │ 通过注册表查找 builder
                ▼
┌────────────────────────────────────────────────────────────┐
│ 适配层 config/ database/ oss/ transport/ cache/             │
│  子包 init() 自注册 → 委托 lulu-ext / go-crud 插件           │
└───────────────┬────────────────────────────────────────────┘
                ▼
┌────────────────────────────────────────────────────────────┐
│ lulu.App（最终运行的应用，统一生命周期/优雅停机）            │
└────────────────────────────────────────────────────────────┘
```

---

## 2. 三层职责划分

| 层 | 目录 | 职责 | 是否依赖业务库 |
|----|------|------|---------------|
| 契约层 | `conf/` | 定义 Schema + 生成 Go 代码 | 否（仅 protobuf runtime） |
| 聚合层 | 根包 `bootstrap.go`/`*_builder.go` | 解析配置、串联 builder、组装 `lulu.App`、生命周期管理 | 是（`lulu` 等） |
| 适配层 | `config/` `database/` `oss/` `transport/` `cache/` | 把单个子系统配置桥接为插件实例 | 是（按子系统） |

---

## 3. 注册表模式（核心扩展机制）

### 3.1 类型常量（`types.go`）
每域一组小写 key 常量，保证 conf 扩展名与注册 key 一致：
- `ServerTypeHTTP`、`ConfigTypeFile`/`ConfigTypeNacos`、`LoggerTypeSlog`、`TracerTypeOTLP`、`MetricsTypePrometheus`/`MetricsTypeOTLP`、`StorageTypeMinio`/`StorageTypeS3`、`CacheTypeLocal`/`CacheTypeRedis`、`DatabaseTypeGorm`/`DatabaseTypeElasticsearch`。

### 3.2 注册表 API（`builder.go`）
每域提供：
- `MustRegisterXxxBuilder(typ, fn)`：`init()` 用，重复/未初始化则 panic。
- `getXxxBuilder(typ) (fn, error)`：`sync.RWMutex` 保护查找，未注册报错。
- `ListXxxBuilders() []string`：自省（排序返回已注册类型）。

> 注：扩展指南中各适配子包文档已详述具体签名；此处为统一抽象。

### 3.3 自注册用法
```go
import _ "github.com/kalandramo/lulu-bootstrap/transport/http"
import _ "github.com/kalandramo/lulu-bootstrap/config/nacos"
```
子包 `init()` 内部调用 `bootstrap.MustRegisterXxxBuilder(...)`，无需业务手写注册。

---

## 4. 顶层解析流程（`bootstrap.go`）

`Bootstrap(ctx, cfg)` 顺序解析，逐步累积 `lulu.Option` 与 `cleanup`：

```
resolveApp        → []lulu.Option
resolveLog        → log.Logger + cleanup   （早注册，后续 builder 可写日志）
resolveServer     → []transport.Server + cleanup
resolveConfig     → 混合配置源 + cleanup
resolveTracer     → TracerProvider + cleanup
resolveMetrics    → cleanup
resolveStorage    → map[string]any + cleanup
resolveCache      → map[string]any + cleanup
resolveDatabase   → map[string]any + cleanup
lulu.New(opts...) → app
```

- 任一 `resolveXxx` 失败：回滚已注册 cleanup，向上返回错误。
- 解析顺序依赖：Tracer 先于 Server（tracing 中间件复用全局 TracerProvider）；Log 最先（供其他 builder 日志输出）。

### 4.1 加载与解码
`LoadConfigFromFile(path)` 按扩展名分发：
- `.yaml`/`.yml`：`sigs.k8s.io/yaml` `YAMLToJSON` + `protojson.Unmarshal`。
- `.json`：`protojson.Unmarshal`。
- `.bin`/`.pb`：`proto.Unmarshal`。

### 4.2 运行期 Context（`context.go`）
`Context` 封装：`cfg`、`app`、`cancel`、`cleanup`，以及按名索引的 `storages`/`caches`/`databases` map。
- `Cleanup()`：`sync.Once` 保证幂等，逆序执行 cleanup。
- 取用：`Storage(name)`/`Cache(name)`/`Database(name)` 返回 `any`，业务需类型断言。

---

## 5. 各子系统适配层设计要点

> 详细设计见各子目录文档：`conf/`、`config/`、`database/`、`oss/`、`transport/` 的设计文档。此处仅汇总接口契约。

| 子系统 | 适配目录 | builder 签名 | 委托插件 | cleanup |
|--------|----------|--------------|----------|---------|
| config | `config/file`,`config/nacos` | `func(ctx, *v1.Config)(func(),error)` | `lulu-ext/config/*` | file: Close；nacos: 空 |
| database | `database/gorm`,`database/elasticsearch` | `func(ctx, *v1.Database)(any,func(),error)` | `tx7do/go-crud/*` | gorm: 关连接池；es: 空 |
| oss | `oss/minio`,`oss/s3` | `func(ctx, *v1.Storage)(any,func(),error)` | `lulu-ext/oss/*` | 均空 |
| transport | `transport/http` | `func(*v1.Server)(transport.Server,error)` | `lulu-ext/transport/http` | 优雅 Shutdown |
| cache(聚合) | 根包 `cache_builder.go` | `func(ctx, *v1.Cache)(map[string]any,func(),error)` | `lulu-ext/cache/*` | 由各 builder 返回 |
| log/metrics/tracer/storage | 根包 `*_builder.go` | 见各文件 | `lulu/log`、`otel` 等 | 见实现 |

> `cache` 适配层目录 `cache/` 在仓库中存在；其具体子包结构与上述子系统同构（local/redis），聚合逻辑在 `cache_builder.go`。

---

## 6. 配置契约与代码生成

- `conf/buf.yaml`：module 路径 `protos`。
- `conf/buf.gen.yaml`：managed mode + `go_package_prefix`，`paths=source_relative`，输出 `conf/gen/go`。
- 生成 10 个 `.pb.go`，对应 10 个 `.proto`（bootstrap/app/server/config/log/tracer/metrics/storage/cache/database）。
- `Makefile`：`make api` → `cd conf && buf generate`。

---

## 7. 依赖管理

- 根 `go.mod`：聚合 `lulu`、`lulu-ext`、各 SDK（nacos、minio、aws-sdk-s3、redis、otel、prometheus、fsnotify 等）。
- 子模块独立：`database/gorm`、`database/elasticsearch` 等含独立 `go.mod`，实现依赖隔离（避免 es SDK 进入 gorm 编译图）。
- proto 运行依赖（`google.golang.org/protobuf`）统一由根模块管理（v2 已删 `conf/go.mod`）。

---

## 8. CLI

- `cli.go` 基于 cobra，根命令 `server`，标志 `--conf/-c`（缺省 `config.yaml`）。
- 业务 `main` 调用 `bootstrap.NewRootCmd(flags, runE)`，在 `runE` 内 `bootstrap.Bootstrap(ctx, cfg)`。

---

## 9. 扩展指南（统一范式）

新增一种子系统能力（如新配置源 `etcd`）：
1. `conf/protos/.../config.proto`：`Config` 加 `optional Etcd etcd = N;`。
2. `types.go`：加 `ConfigTypeEtcd`。
3. 新建适配包 `config/etcd/etcd.go`：`init(){ bootstrap.MustRegisterConfigAction(ConfigTypeEtcd, newAction) }`，委托 `lulu-ext/config/etcd`。
4. `buf generate` + 业务 `import _ ".../config/etcd"` 生效。

无需改动聚合层（`resolveConfig` 遍历已设置 optional 时动态查找注册表），符合开闭原则。

---

## 10. 待办 / 一致性备注

1. **破坏性收敛基线**：v2 移除 5 域并重排顶层字段编号，建议以本轮提交为 `buf breaking --against` 新基线。
2. **空 cleanup 补全**：oss(minio/s3)、nacos、elasticsearch 的 cleanup 为空，需补优雅关闭。
3. **HTTP Driver 未消费**：conf `server.proto` 的 `driver`（chi/gin/fiber）当前未对接 `lulu-ext` 对应 Driver，始终用内置 std Driver。
4. **GORM 多实例迁移**：gorm 迁移模型为全局包级注册，多 GORM 实例共享同一迁移集，多库异构场景需改造。
5. **cache 适配层**：`cache/` 目录存在但未在本次逐一展开文档，建议后续补 `cache/需求文档.md`/`设计文档.md`（与 config/database 同构）。
6. **Context 字段一致性**：确保 `context.go` 不残留已移除域（broker/ai/script/workflow）的引用。
