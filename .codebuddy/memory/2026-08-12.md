# 2026-08-12

## lulu-bootstrap conf 模块文档
- 生成 `conf/需求文档.md` + `conf/设计文档.md`（v1），随后因 conf 大规模精简（15→10 proto，移除 ai/broker/registry/script/workflow，顶层 14→9 字段），更新为 **v2**。
- v2 conf 现状：仅 9 子域（app/server/config/logger/tracer/metrics/storage/cache/database）。config 仅 file+nacos；server 仅 http（7 中间件）；logger 仅 slog；database 仅 sql+gorm/elasticsearch。

## lulu-bootstrap 子系统适配层文档（init 自注册模式）
- `config/`（file、nacos）、`database/`（gorm、elasticsearch）、`oss/`（minio、s3）、`transport/`（http）各生成需求+设计文档。
- 统一模式：子包 `init()` 调 `bootstrap.MustRegisterXxxBuilder(typeKey, newBuilder)`，`import _` 启用；委托 `lulu-ext/*` 或 `tx7do/go-crud/*` 插件；返回 cleanup 闭包。
- 注意点：旧顶层 `config_builder.go`/`database_builder.go`/`config_builder.go` 已删除，聚合逻辑在 `bootstrap` 包；oss 与 nacos/es 的 cleanup 多为空函数；transport/http 内置 std Driver（net/http），conf server.proto 的 driver 字段（chi/gin/fiber）当前未消费；gorm 迁移模型为全局包级注册（多实例共享，有隐患）。

## 全局文档
- 生成仓库根 `需求文档.md` + `设计文档.md`（v2）。三层架构：契约层(conf) + 聚合层(bootstrap 根包 *_builder.go) + 适配层(config/database/oss/transport/cache)。
- 核心机制：builder 注册表（types.go 常量 + MustRegisterXxxBuilder + getXxxBuilder RWMutex）+ 子包 init() 自注册 + `import _` 启用。
- Bootstrap 顺序：resolveApp→Log→Server→Config→Tracer→Metrics→Storage→Cache→Database→lulu.New。Context 封装 storages/caches/databases（any 按名索引），Cleanup 用 sync.Once 逆序回收。
- 待办：cache/ 适配层未单独展开文档（与 config/database 同构）；多个 cleanup 为空；driver 字段未消费；gorm 全局迁移模型隐患。
